# Local XAMPP: send all requests into public_html.
# On Namecheap this file sits above the document root and is unused.
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteRule ^public_html/ - [L]
  RewriteRule ^(.*)$ public_html/$1 [L]
</IfModule>
